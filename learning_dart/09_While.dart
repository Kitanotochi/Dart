// Цикл "while" в языке Dart

void main() {
  int counter = 0;
  // Цикл выполнился десять раз, но условие проверяется перед входом в цикл
  while(counter < 10) { 
    print("Hello Dart $counter");
    counter++;
  }
}